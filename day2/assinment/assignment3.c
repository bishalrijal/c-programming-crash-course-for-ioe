/* fix the error of the following program 
try to solve the problem with out compilation first 
after you think, you are done then compile the program

try to find syntactical error, identifier naming error and logical error (compare what you except from the code and what you see on output ) 
*/
#include<stdio.h>
#include<conio.h>
#define pi 3.1425;
int main (void)
{
	float r1, r2, r3, a1, a2, a3;
	printf ("enter radius of three different circles: \t")
	scanf ("%f%f%f",&r1, &r2,);
	a1= pi*r1*r1;
	2a= pi*r2*r2;
	a3= pir3r3;
	printf("area of the first circle= %f",a1);
	printf("\n area of the second circle=%.2f",2a);
	printf ("\n area of the third circle=%.2f",a3);
	getch () ;
	return 0;
	
}

